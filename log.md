# Wiki — Log

_Append-only history. Never edit past entries; add new ones at the bottom. Each entry is headed
`## [YYYY-MM-DD HH:mm:ss] <type> | <title>` where `<type>` is one of: ingest, contradiction, lint,
acquire, schema. See `CLAUDE.md` → "Log format"._
